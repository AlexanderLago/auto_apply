# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0        # .env loading
pydantic>=2.0.0             # data models & validation
requests>=2.31.0            # HTTP calls (Greenhouse, Lever, Adzuna APIs)

# ── LLM ───────────────────────────────────────────────────────────────────────
anthropic>=0.25.0           # Claude (tailoring, parsing, scoring)
openai>=1.30.0              # OpenAI-compatible client — used for Groq, Cerebras, Gemini
google-generativeai>=0.7.0  # Native Gemini SDK (fallback for gemini type)

# ── Resume parsing ────────────────────────────────────────────────────────────
pdfplumber>=0.10.0          # PDF text extraction
python-docx>=1.1.0          # DOCX read/write

# ── LinkedIn (unofficial Voyager API) ─────────────────────────────────────────
linkedin-api>=2.1.0             # pip install linkedin-api

# ── Browser automation (optional — Easy Apply) ────────────────────────────────
playwright>=1.44.0          # Headless browser for LinkedIn Easy Apply
                            # After install: `playwright install chromium`
playwright-stealth>=0.0.9   # Patches browser fingerprint to avoid bot detection
# python-jobspy>=1.1.0      # Requires Python>=3.10 — skip for now (using IndeedScraper instead)

# ── Dashboard (optional) ──────────────────────────────────────────────────────
streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.18.0          # Interactive charts for dashboard

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0.0
